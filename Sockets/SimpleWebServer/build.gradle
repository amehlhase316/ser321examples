plugins {
    id 'application'
}

apply plugin: 'application'

application {
    mainClassName = 'SimpleWebServer'
    description = "Simple Web server Example"
}

run {
    // default arguments
    args '9099' // port
}
